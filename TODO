temp-disable ss segment after writing to it. (until a write to (e)sp)


If there are many REP operations in the code, it could be that GUI-Events can't get executed.
  Probably this won't get fixed!

PCI
Opcodes::interrupt
ATA
VM86

PIT (ReactOS 0.4.0)
PS2
RS232(?)


PCI: Save busmaster base address instead of fetching it each time

timing issues, which are resulting into some heavy bugs.

Somewhen in a few centuries:
ACPI (win2k, ...)
AMD64


