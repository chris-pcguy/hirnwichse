If in code are many REP operations, it could be that GUI-Events can't get executed.
  Probably this won't get fixed!

Do character write over pygame_events? (cpuThread: videoMmArea-->EventList;; mainThread: EventList-->doOutput)

PCI
Opcodes::interrupt
ATA
VM86
